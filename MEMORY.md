# ذاكرة المشروع

- المستودع الأصلي كان يحتوي على `index.html.html` فقط؛ تم اعتماد `index.html` كمدخل قياسي.
- الخطأ الأكبر في الإحصاءات كان استخدام `state.target` بدل `state.targetMoney`.
- مؤقتا القلاية وموزع المشروبات كانا يضبطان الحالة إلى `empty` مباشرة بعد اكتمال المنتج؛ النسخة الجديدة تحافظ على الحالة `cooked`/`filled` حتى الجمع.
- توليد الزبائن أصبح دوريًا حتى لا يتوقف بعد امتلاء الطابور.
- تم اختيار نظام شخصيات خفيف بلا صور خارجية للحفاظ على قابلية التشغيل الساكنة.
